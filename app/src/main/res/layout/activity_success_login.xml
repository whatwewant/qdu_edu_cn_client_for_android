<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.portalsign.app.SuccessLogin">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:id="@+id/imageView"
        android:src="@drawable/logo"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:layout_above="@+id/signOut"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="91dp" />


    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="注销"
        android:id="@+id/signOut"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="30dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="130dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0.0.0.0"
        android:textSize="20sp"
        android:id="@+id/ipView"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/signOut" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="00:00:00"
        android:id="@+id/timeView"
        android:textSize="20sp"
        android:layout_above="@+id/ipView"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start VPN"
        android:id="@+id/startVpnBtn"
        android:layout_alignTop="@+id/signOut"
        android:layout_marginTop="61dp"
        android:layout_alignLeft="@+id/signOut"
        android:layout_alignStart="@+id/signOut"
        android:layout_toLeftOf="@+id/ipView" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Shadowsocks"
        android:id="@+id/shadowsocksBtn"
        android:layout_alignTop="@+id/startVpnBtn"
        android:layout_alignRight="@+id/signOut"
        android:layout_alignEnd="@+id/signOut" />


</RelativeLayout>
